#!/usr/bin/env node
// Minimal placeholder post-build script
console.log('post-build: placeholder running');
// Add real post-build tasks here if needed (copying files, cleanup, etc.)
console.log('post-build: complete');
process.exit(0);
